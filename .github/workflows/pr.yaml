name: Run Pull Request Checks

on:
  pull_request:
    branches: main

jobs:

  check:
    name: Check
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Ruff Check
        uses: jpetrucciani/ruff-check@0.5.2
  